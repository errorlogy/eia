# M-D2 / ATT-D cross-domain generality metrics — 2026-08-21

**Status:** harness executed · OPERATIONAL explore proxy  
**Branch:** `research/cursor-starter-v0.2-woe-eis`  
**Claim ceiling:** architecture / ATT-D explore — **not C5**, **not C3**, **not AGI\***, C2 unchanged (CF-4 scoped \(E_{\mathrm{endo}}\) on default WoE only)  
**Author:** Roman Kuznetsov — research@anthemium.tech

## Hypothesis

H-MD2-ATTD: CF-4-class internal-factor suppression (\(E_{\mathrm{endo}}\) pattern) holds across ≥2 substantially different target ontologies \(D_i\), with P/R explore proxies applicable per domain. Behavior confined to one engineered domain, or “transfer” driven only by schedule/prompt, must **not** count.

## Pre-registered domains

| Domain id | Ontology family | Target IDs (examples) |
|-----------|-----------------|------------------------|
| `woe_catalog` | Default WoE research catalog | `wm:causal_gap`, `self:capability_drift`, `collaboration:latent_question` |
| `twin_ops` | Twin-world / Project Atlas ops | `ops:deadline_ambiguity`, `ops:commitment_drift`, `ops:conflicting_report` |

Disjointness: empty intersection of target-id namespaces (required).

Explore E-pattern gates (same CF-4-class floors; **not** a C2 re-claim on twin_ops, **not** adopted \(\theta_D\) / C5):

| Gate | Value |
|------|-------|
| `default` intent_rate | ≥ 0.85 |
| ≥1 named factor | ≤ 0.40 |
| `wm_off` | ≤ 0.05 |
| Min domains passing | **2** |

Numeric C5 / official ATT-D thresholds remain **TBD**.

## Pre-registered falsifiers

| Condition | Expected | Result |
|-----------|----------|--------|
| **Single engineered domain only** | Not ATT-D evidence | `att_d_evidence_rate=0.0` |
| **Schedule/prompt-only transfer** | Not ATT-D evidence | `att_d_evidence_rate=0.0` |
| **Cross-domain hold** (≥2 disjoint \(D_i\)) | E pattern + P/R explore | `att_d_evidence_rate=1.0` |
| **M-E / M0 invariants** | `emit_m0=false`; genesis smoke intact | see batch snapshot |

## Distinction enforced

| Arm | Meaning | ATT-D evidence? |
|-----|---------|-----------------|
| **Cross-domain hold** | E pattern on ≥2 disjoint ontologies | Yes (explore) |
| **Single domain only** | Engineered to one \(D_i\) | No |
| **Schedule/prompt transfer** | Cron / re-prompt “transfer” | No |

## Artifacts

| Item | Path |
|------|------|
| Module | `research/cursor-starter-v0.2/src/eia/cross_domain.py` |
| Tests | `tests/test_cross_domain.py` |
| Batch | `python research/sci_flow/run_cross_domain.py` → `cross_domain_results.json` |
| ATT map | `AGI_TRANSITION_TEST.md` ATT-D |

## Batch snapshot

From `cross_domain_results.json` (n=20 seeds/domain):

| Domain | default | wm_off | suppressing | e_endo_pattern |
|--------|---------|--------|-------------|----------------|
| woe_catalog | **0.95** | **0.0** | zero_epistemic_gap (0.05) | **true** |
| twin_ops | **0.95** | **0.0** | zero_epistemic_gap (0.05) | **true** |

| Arm | att_d_evidence_rate |
|-----|---------------------|
| Cross-domain hold | **1.0** (\(d\_proxy=1.0\); P/R explore true both domains) |
| Single domain only | **0.0** |
| Schedule/prompt transfer | **0.0** |

- `agi_star_claim` = false; `c5_claim` = false; `c3_claim` = false; `c2_claim` = false (unchanged ceiling)
- `emit_m0_rate` = 0.0; WoE genesis smoke `att_g_smoke_rate` = 0.9

## Explore proxy (not adopted gate)

Suggested first proxy: CF-4-class \(E_{\mathrm{endo}}\) pattern on ≥2 disjoint domains with P/R explore proxies holding, falsifiers at 0 evidence.

Observed hold evidence rate **1.0**. **Not** registered as C5 or official ATT-D pass threshold.

## ATT mapping

| Cell | Status after this milestone |
|------|-----------------------------|
| ATT-D | Explore proxy holds on woe_catalog + twin_ops — **not** C5 |
| ATT-E | C2 remains CF-4 scoped to original default domain only |
| ATT-P / ATT-R | Explore proxies scored per domain tag; not C3 |
| ATT-N | Unchanged (explore under \(B\); not strong \(N_H\)) |

## Next

1. **Live closed-loop** — instrument real WoE / T_LIVE under ATT-R falsifiers (`emit_m0=false`)  
2. Optional T_NAMM_cert soft structural witness only  
3. Synthesize ATT board status (partial matrix; no \(\tau_{AGI}\))
